import React from 'react';

const Residentslist = ({urls}) => {
    return (
        <div>
            {
                url?.map() 
            }
        </div>
    );
}

export default Residentslist;
